<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Share Reads</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="css/style6.css">
</head>
<body>
    <div class="container my-5">
        <div class="position-relative p-5 text-center text-muted bg-body border border-dashed rounded-5">
            <% if(typeof user.profileImage != "undefined") { %>
                <img src="<%= user.profileImage %>" alt="<%= user.name %>" class="img">
            <% } else { %>
                <img src="images/user.png" alt="<%= user.name %>" class="img">
            <% } %>

          <h1 class="text-body-emphasis"> <%= user.name %> </h1>
          <p class="col-lg-6 mx-auto mb-3">
            Division: <%= user.division %>
          </p>
          <p class="col-lg-6 mx-auto mb-3">
            Address: <%= user.address %>
          </p>
          <p class="col-lg-6 mx-auto mb-3">
            Email: <%= user.username %>
          </p>
          <p class="col-lg-6 mx-auto mb-3">
            Phone: <%= user.phone %>
          </p>
        </div>
      </div>

      <div class="footer-padding"></div>
      <div class="footer">
        <p>Made with ❤️ by Durjoy</p>
      </div>
    </div>
    
</body>
</html>