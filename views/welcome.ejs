<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome to Share Reads</title>
    <link rel="stylesheet" href="css/modern-style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="icon" href="images/book.png" type="image/png">
    <style>
        /* Public Navigation Bar */
        .public-navbar {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            padding: 16px 0;
            box-shadow: 0 4px 16px rgba(139, 127, 199, 0.1);
            z-index: 1000;
        }
        
        .nav-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 32px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .nav-logo {
            font-size: 24px;
            font-weight: 700;
            color: #8b7fc7;
            text-decoration: none;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .nav-buttons {
            display: flex;
            gap: 16px;
            align-items: center;
        }
        
        .btn-nav {
            padding: 10px 24px;
            border-radius: 12px;
            font-weight: 600;
            text-decoration: none;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 8px;
            font-size: 15px;
        }
        
        .btn-login {
            color: #8b7fc7;
            background: rgba(139, 127, 199, 0.1);
        }
        
        .btn-login:hover {
            background: rgba(139, 127, 199, 0.2);
            transform: translateY(-2px);
        }
        
        .btn-signup {
            color: white;
            background: linear-gradient(135deg, #8b7fc7 0%, #a89fd9 100%);
            box-shadow: 0 4px 16px rgba(139, 127, 199, 0.3);
        }
        
        .btn-signup:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(139, 127, 199, 0.4);
        }
        
        .hero-modern {
            padding-top: 80px;
        }
        
        .hero-cta-primary {
            transition: all 0.3s ease;
        }
        
        .hero-cta-primary:hover {
            transform: translateY(-4px) scale(1.02);
            box-shadow: 0 12px 40px rgba(139, 127, 199, 0.5) !important;
        }
        
        .hero-cta-secondary {
            transition: all 0.3s ease;
        }
        
        .hero-cta-secondary:hover {
            transform: translateY(-4px) scale(1.02);
            background: white !important;
            box-shadow: 0 12px 40px rgba(0, 0, 0, 0.2) !important;
        }
        
        .mobile-menu-btn {
            display: none;
            background: none;
            border: none;
            font-size: 24px;
            color: #8b7fc7;
            cursor: pointer;
        }
        
        @media (max-width: 768px) {
            .nav-buttons {
                position: fixed;
                top: 70px;
                left: 0;
                right: 0;
                background: rgba(255, 255, 255, 0.98);
                flex-direction: column;
                padding: 20px;
                box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
                transform: translateY(-100%);
                opacity: 0;
                transition: all 0.3s ease;
                pointer-events: none;
            }
            
            .nav-buttons.active {
                transform: translateY(0);
                opacity: 1;
                pointer-events: all;
            }
            
            .mobile-menu-btn {
                display: block;
            }
            
            .btn-nav {
                width: 100%;
                justify-content: center;
            }
        }
    </style>
</head>
<body>
    <!-- Public Navigation Bar -->
    <nav class="public-navbar">
        <div class="nav-container">
            <a href="/" class="nav-logo">
                <img src="images/Share Reads .png" alt="Share Reads Logo" style="height: 40px;">
            </a>
            <button class="mobile-menu-btn" id="mobileMenuBtn">
                <i class="fa-solid fa-bars"></i>
            </button>
            <div class="nav-buttons" id="navButtons">
                <a href="/login" class="btn-nav btn-login">
                    <i class="fa-solid fa-sign-in-alt"></i>
                    Login
                </a>
                <a href="/signup" class="btn-nav btn-signup">
                    <i class="fa-solid fa-user-plus"></i>
                    Sign Up
                </a>
            </div>
        </div>
    </nav>

    <section class="hero-modern">
        <div class="hero-content">
            <p class="hero-subtitle">📚 Welcome to Share Reads</p>
            <h1 class="hero-title">Give your old books a new life!<br>Share them with ease on our platform!</h1>
            
            <div style="animation: fadeInUp 0.8s ease 0.4s both; display: flex; gap: 20px; justify-content: center; flex-wrap: wrap; margin-top: 2.5rem;">
                <a href="/signup" class="btn-modern hero-cta-primary" style="font-size: 18px; padding: 18px 56px; text-decoration: none; background: linear-gradient(135deg, #8b7fc7 0%, #a89fd9 100%); box-shadow: 0 8px 32px rgba(139, 127, 199, 0.4); font-weight: 700; letter-spacing: 0.5px;">
                    <i class="fa-solid fa-rocket"></i> Get Started →
                </a>
                <a href="/login" class="btn-modern hero-cta-secondary" style="font-size: 18px; padding: 18px 56px; text-decoration: none; background: rgba(255, 255, 255, 0.95); color: #8b7fc7; backdrop-filter: blur(10px); box-shadow: 0 8px 32px rgba(0, 0, 0, 0.15); font-weight: 700; border: 2px solid rgba(139, 127, 199, 0.3);">
                    <i class="fa-solid fa-sign-in-alt"></i> Login
                </a>
            </div>
            
            <div style="margin-top: 3rem; display: flex; gap: 2rem; justify-content: center; flex-wrap: wrap; animation: fadeInUp 0.8s ease 0.6s both;">
                <div style="text-align: center;">
                    <div style="font-size: 32px; font-weight: 700; color: white;">1000+</div>
                    <div style="font-size: 14px; color: rgba(255,255,255,0.8);">Books Shared</div>
                </div>
                <div style="text-align: center;">
                    <div style="font-size: 32px; font-weight: 700; color: white;">500+</div>
                    <div style="font-size: 14px; color: rgba(255,255,255,0.8);">Happy Readers</div>
                </div>
                <div style="text-align: center;">
                    <div style="font-size: 32px; font-weight: 700; color: white;">8</div>
                    <div style="font-size: 14px; color: rgba(255,255,255,0.8);">Divisions</div>
                </div>
            </div>
        </div>
    </section>

    <script>
        // Mobile menu toggle
        const mobileMenuBtn = document.getElementById('mobileMenuBtn');
        const navButtons = document.getElementById('navButtons');
        
        mobileMenuBtn.addEventListener('click', function() {
            navButtons.classList.toggle('active');
            const icon = this.querySelector('i');
            if (navButtons.classList.contains('active')) {
                icon.classList.remove('fa-bars');
                icon.classList.add('fa-times');
            } else {
                icon.classList.remove('fa-times');
                icon.classList.add('fa-bars');
            }
        });
    </script>
</body>
</html>